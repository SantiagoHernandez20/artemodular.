{
  "name": "artemodular-backend",
  "version": "1.0.0",
  "description": "Backend para ArteModular - Envío de emails de contacto",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && npm audit fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["express", "nodemailer", "cors", "artemodular"],
  "author": "ArteModular",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
